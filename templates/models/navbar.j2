<nav class="bg-indigo-600 text-white shadow-md">
    <div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
        <!-- Logo / Accueil -->
        <a href="{{ url_for('index') }}" class="text-2xl font-bold hover:text-indigo-200 transition">
            Theytube
        </a>

        <!-- Liens -->
        <div class="space-x-6">
            <a href="{{ url_for('index') }}" class="hover:text-indigo-200 transition">Accueil</a>
            <a href="{{ url_for('history.watch_history') }}" class="hover:text-indigo-200 transition">Historique</a>
        </div>
    </div>
</nav>